Support Secure Compute networks when creating, forking, and updating sandboxes with `network_id`. Expose the attached network ID on sandbox handles and allow `update(network_id=None)` to disconnect a sandbox.
