# Vercel Blob Python SDK

This package will provide synchronous and asynchronous file-like APIs for Vercel Blob.

Install it independently with `pip install vercel-blob`. The asynchronous API will live
at `vercel.blob`, with its synchronous counterpart at `vercel.blob.sync`.

The package currently contains only those import surfaces. Blob operations will be added
in follow-up changes.
