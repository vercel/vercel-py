[project]
name = "vercel-apscheduler-example-cleanup"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "APScheduler>=3.10.4,<4",
    "vercel-apscheduler>=0.1.0",
    "fastapi>=0.115.6,<0.116",
]

[tool.uv]
package = false
